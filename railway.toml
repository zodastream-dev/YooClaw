[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm install -g @tencent-ai/codebuddy-code && npx tsx server/index.ts"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
