[build]
builder = "NIXPACKS"

[deploy]
# Direct HTTP API mode — no CLI needed
startCommand = "npx tsx server/index.ts"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
